use crate::frac::Frac;
use std::collections::HashSet;

struct Solver {
    ans: Frac,
}

impl Solver {
    pub fn new(ans: Frac) -> Solver {
        Solver {
            ans,
        }
    }
}

#[allow(dead_code)]
pub fn solver(nums: Vec<u32>, ans: Frac) -> String {

    return String::new();
}
